<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rl_load_dialog"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_centerInParent="true"
    android:paddingLeft="30dp"
    android:paddingRight="30dp"
    android:background="@drawable/shape_square_load_gray"
    android:paddingTop="30dp"
    android:paddingBottom="30dp"
    android:visibility="visible">
    <!--android:background="@drawable/progress_custom_bg"-->
    <!-- <ProgressBar
         android:id="@+id/iv_progress_dialog"
         android:layout_width="50dp"
         android:layout_height="50dp"
         android:layout_alignParentTop="true"
         android:layout_centerHorizontal="true"
         android:indeterminate="false"
         android:indeterminateDuration="1000"
         android:indeterminateDrawable="@drawable/dialog_style_xml_icon" />-->
    <ProgressBar

        android:id="@+id/iv_progress"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:indeterminate="false"
        android:indeterminateDuration="5000"
        android:indeterminateTint="@color/white" />

    <TextView
        android:id="@+id/tv_load_dialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/iv_progress"
        android:layout_centerHorizontal="true"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="5dp"
        android:text="vyvvvvtt"
        android:textColor="@color/white"
        android:textSize="@dimen/dp_20" />
</RelativeLayout>